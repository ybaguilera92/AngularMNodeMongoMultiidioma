import { Title } from '@angular/platform-browser';
export interface Usuario{
    id?: number,
    username?: string,
    name?: string,
    last_name?: string,
    email?: string,
    password?: string,
    ci?: string,
    phone?: string,
    address?: string,
}